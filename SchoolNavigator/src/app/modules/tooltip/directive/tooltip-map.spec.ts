import { TooltipMap } from './tooltip-map';

describe('TooltipMap', () => {
  it('should create an instance', () => {
    const directive = new TooltipMap();
    expect(directive).toBeTruthy();
  });
});
