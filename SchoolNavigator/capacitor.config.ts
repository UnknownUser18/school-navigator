import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.navigator.school',
  appName: 'School Navigator',
  webDir: 'dist/SchoolNavigator/browser'
};

export default config;
